import React from 'react';
import { Flex, Text } from '@chakra-ui/react';

const ControlView = () => (
  <Flex
    flexDirection="row"
    justifyContent="center"
    alignItems="center"
    w="100%"
  >
    <Text>Autopilot Camera</Text>
  </Flex>
);

export default ControlView;
